{
  "name": "mingru",
  "version": "0.14.0",
  "description": "SQL Generator for dd-models",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "author": "Mgen (https://www.mgenware.com)",
  "homepage": "https://github.com/mgenware/mingru",
  "bugs": "https://github.com/mgenware/mingru/issues",
  "keywords": [
    "mingru"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mgenware/mingru.git"
  },
  "license": "MIT",
  "scripts": {
    "test": "yarn run clean && yarn run compile && yarn run lint && yarn run t",
    "t": "mocha dist_tests/**/*.test.js",
    "build": "yarn test",
    "compile": "tsc -b tests",
    "dev": "yarn run clean && tsc -b tests -w",
    "prepublishOnly": "yarn run test",
    "lint": "tslint --project tsconfig.json --config tslint.json",
    "clean": "del-cli dist && del-cli dist_tests"
  },
  "engines": {
    "node": ">=8.6.0"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.7.5",
    "dd-models": "^0.21.0",
    "del-cli": "^3.0.0",
    "mocha": "^6.2.0",
    "tempy": "^0.3.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "del": "^5.1.0",
    "m-fs": "^2.3.2",
    "sql-escape-string": "^1.1.0",
    "throw-if-arg-empty": "^2.1.0",
    "to-type-string": "^1.0.1"
  }
}
